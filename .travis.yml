language: python

python:
  - "3.5"
  - "3.6"

env:
  - DJANGO_VERSION=2.1
  - DJANGO_VERSION=2.2

# command to install dependencies
install:
  - pip install -r requirements.txt

# command to run tests
script:
  - ./manage.py test
